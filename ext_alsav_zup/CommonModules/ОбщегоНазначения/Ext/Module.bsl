
&Вместо("ЭтоДокумент")
Функция АЛСАВ_ЭтоДокумент(ОбъектМетаданных)
	// Вставить содержимое метода.
	Попытка
		Результат = ПродолжитьВызов(ОбъектМетаданных);
	Исключение
		Возврат ЛОЖЬ;
	КонецПопытки;
	Возврат Результат;
КонецФункции