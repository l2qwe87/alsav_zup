
&Вместо("ПриЗаписи")
Процедура АЛСАВ_ПриЗаписи(Отказ, Замещение)
	// Вставить содержимое метода.  
	
	НаборЗаписей = ЭтотОбъект;
	ФизическиеЛица = РегистрыБЗК.ЗначенияОтбораЗаписейНабора(НаборЗаписей, Замещение, "Физлицо");
	Если ФизическиеЛица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого физЛицо из ФизическиеЛица Цикл
		Справочники.Сотрудники.ЗаполнитьДопРеквФизЛицо(физЛицо);
	КонецЦИкла;
	
	ПродолжитьВызов(Отказ, Замещение);
КонецПроцедуры
